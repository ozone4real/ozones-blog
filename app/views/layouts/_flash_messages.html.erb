<% unless flash.empty? %>
    <script type="text/javascript">
        console.log("Yeeeeeeah")
        <% flash.each do |f| %>
        <% type = f[0].to_s %>
        toastr['<%= type %>']('<%= f[1] %>');
        <% end %>
    </script>
<% end %>