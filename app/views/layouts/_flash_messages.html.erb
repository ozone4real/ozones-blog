<% unless flash.empty? || flash[:shown] %>
  <script type="text/javascript">
  $('#toast-container').hide()
    <% flash.each do |f| %>
    <% type = f[0].to_s %>
    toastr['<%= type %>']('<%= f[1] %>');
    <% end %>
  </script>
<% end %>
